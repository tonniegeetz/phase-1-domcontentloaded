// Your code goes here

document.addEventListener('DOMContentLoaded', function () {
  const text = document.getElementById('text');
  text.textContent = 'This is really cool!';
});

//there is also a load event that is used when everything including the CSS and the images are completely loaded
